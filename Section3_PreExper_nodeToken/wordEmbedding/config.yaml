# model_name: cbow, skipgram, combined
model_name: combined
# 是否需要筛选数据，即将 NL 与 L 分开处理（filter=True）
filter: True
model_dir: weights/combined_preExper
epochs: 300

dataset: RandomGenerate
# 处理后的带有 label 的 R-tree 的 node token 上下文
data_dir: /mnt/d/project/Java/rtree_construct/preExperData/RTreeTokenContext
train_batch_size: 96
val_batch_size: 96
shuffle: True

optimizer: Adam
learning_rate: 0.025

train_steps: 
val_steps:
checkpoint_frequency: 


